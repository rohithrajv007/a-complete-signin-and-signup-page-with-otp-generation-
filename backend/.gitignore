
/generated/prisma
# ===================================================================
# General
# ===================================================================

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
# Never commit your secret keys or environment-specific settings!
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Dependency directories
# These are installed via 'npm install' and can be very large
node_modules/

# ===================================================================
# Backend (Node.js / Express / Prisma)
# ===================================================================

# Prisma
# The 'generated' client can be recreated with 'npx prisma generate'
# and doesn't need to be in version control.
/backend/node_modules/.prisma/client

# ===================================================================
# Frontend (Vite + React)
# ===================================================================

# Build output directory
# This folder is generated when you run 'npm run build'
/frontend/dist

# Vite-specific cache and log files
/frontend/.vite
/frontend/coverage

# ===================================================================
# IDE / OS Specific
# ===================================================================

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
